# 因子库 J：非线性变换与混合结构（NL01–NL25）

> 对已有/本库因子做非线性与门控变换。依据几乎全部为 [实践]/[推测]：
> LLM 头脑风暴范式在此类因子上**同质化与过拟合风险最高**，优先级最低。

### NL01 动量的凸性（平方保留符号）
- **式**: `sign(ret_20d) * (ret_20d)^2`
- **意**: 放大大幅动量、压缩小幅噪音
- **据**: [推测] | **向**: ? | **数**: 现有

### NL02 反转的凹变换
- **式**: `-sign(ret_5d) * sqrt(abs(ret_5d))`
- **意**: 压缩极端反转（连续跌停股反弹买不到）
- **据**: [推测] | **向**: + | **数**: 现有

### NL03 动量排名的极值聚焦
- **式**: `if(abs(cs_rank(ret_20d)-0.5)>0.4, cs_rank(ret_20d), 0.5)`
- **意**: 只取头尾 20% 的动量信息，中间置为中性
- **据**: [实践][推测] | **向**: ? | **数**: 现有

### NL04 因子分位离散化（turnover 五档）
- **式**: `floor(cs_rank(ts_mean(turnover,20))*5)`
- **意**: 换手分档（树模型天然能切，此因子服务于线性诊断）
- **据**: [实践] | **向**: ? | **数**: 现有

### NL05 波动×反转的门控
- **式**: `if(cs_rank(ts_std(ret,20))<0.3, -ret_5d, 0)`
- **意**: 只在低波股上做反转（高波股反转不可靠假说）
- **据**: [推测] | **向**: + | **数**: 现有

### NL06 高波股动量门控
- **式**: `if(cs_rank(ts_std(ret,20))>0.7, ret_20d, 0)`
- **意**: 高波股信动量（赌性票趋势延续）
- **据**: [推测] | **向**: ? | **数**: 现有

### NL07 双因子 zscore 合成（反转+获利盘）
- **式**: `zscore(-ret_5d) + zscore(-winner_rate)`
- **意**: 超跌+低获利盘=干净的反转结构
- **据**: [推测] | **向**: + | **数**: 现有

### NL08 反转与流动性的条件合成
- **式**: `if(LQ01 池内前 30%, zscore(-ret_5d), 0)`
- **意**: 只在流动性尚可的票上做反转（买得到、卖得出）
- **据**: [实践][推测] | **向**: + | **数**: 现有

### NL09 因子拥挤度惩罚
- **式**: `cs_rank(-ret_5d) * (1 - cs_rank(ts_mean(turnover,5)))`
- **意**: 反转信号在冷门票上更纯（无资金踩踏）
- **据**: [推测] | **向**: + | **数**: 现有

### NL10 秩相关合成（动量×量确认）
- **式**: `cs_rank(ret_20d) * cs_rank(ts_sum(if(ret>0,volume,0),20)/ts_sum(volume,20))`
- **意**: 涨时放量占比 × 动量（量价共振）
- **据**: [推测] | **向**: ? | **数**: 现有

### NL11 三因子投票（反转/获利盘/换手）
- **式**: `sign(zscore(-ret_5d)) + sign(zscore(-winner_rate)) + sign(-zscore(turnover_3d))`
- **意**: 多数投票式合成（离散，抗噪）
- **据**: [推测] | **向**: + | **数**: 现有

### NL12 条件期望差（盈利股 vs 亏损股的估值行为）
- **式**: `if(pe_ttm>0, cs_rank(-pb), cs_rank(pb))`
- **意**: 盈利股买便宜、亏损股买贵（壳价值）——分段逻辑
- **据**: [推测]（微盘池壳价值假说） | **向**: ? | **数**: 现有

### NL13 时序分位变换（自身历史分位）
- **式**: `ts_percentile(ts_mean(turnover,5), 252)`
- **意**: 换手的自身历史分位（各票基数不同，标准化）
- **据**: [实践] | **向**: ? | **数**: 现有

### NL14 双重排名（时序×截面）
- **式**: `cs_rank(NL13)`
- **意**: 自身分位再横向比
- **据**: [实践]（变体） | **向**: ? | **数**: 现有

### NL15 因子的半衰期加权
- **式**: `0.5*ret_5d_rank + 0.3*ret_10d_rank + 0.2*ret_20d_rank`（加权动量）
- **意**: 近因加权动量（与 MO29 对称）
- **据**: [实践]（变体） | **向**: ? | **数**: 现有

### NL16 负向交互（小市值×低波）
- **式**: `cs_rank(-circ_mv) * cs_rank(-ts_std(ret,20))`
- **意**: 小且稳=池内核心资产象限
- **据**: [推测] | **向**: + | **数**: 现有

### NL17 象限指示（四象限 one-hot）
- **式**: `if(高波 and 低价, 1, 0)` 等四个指示器
- **意**: 显式象限特征（供模型显式分裂）
- **据**: [推测] | **向**: ? | **数**: 现有

### NL18 因子距离度量（对池内均值向量）
- **式**: `-euclidean(zscore([ret_5d, turnover, vol, winner]))`（多维异常度取负=正常度）
- **意**: 各维度都普通的票 vs 某维度极端的票
- **据**: [推测] | **向**: ? | **数**: 现有

### NL19 max 变换（多窗口最优）
- **式**: `max(cs_rank(-ret_5d), cs_rank(-ret_10d))`
- **意**: 任一窗口超跌即触发（宽触发式反转）
- **据**: [推测] | **向**: + | **数**: 现有

### NL20 因子强度的时序确认
- **式**: `if(ts_delay(sign(因子),1)==sign(因子), 因子, 0)` 应用到反转因子
- **意**: 连续两日同向信号才有效（信号持续性过滤）
- **据**: [实践][推测] | **向**: + | **数**: 现有

### NL21 非线性市值权重
- **式**: `因子 * circ_mv^(-0.5)`（市值负幂加权）
- **意**: 显式向小市值倾斜（把市值效应编进因子）
- **据**: [推测] | **向**: ? | **数**: 现有

### NL22 极端值截断后的排名
- **式**: `cs_rank(clip(ret_20d, -0.3, 0.3))`
- **意**: 截断防极端（连续涨停股的动量失真）
- **据**: [实践] | **向**: ? | **数**: 现有

### NL23 因子相关性惩罚版合成
- **式**: `zscore(-ret_5d) - 0.5*zscore(ret_60d)`（正交化合成）
- **意**: 短反转扣掉与长动量的公共成分
- **据**: [推测] | **向**: ? | **数**: 现有

### NL24 波动调整后的因子暴露
- **式**: `(cs_rank(-ret_5d) - 0.5) / (cs_rank(ts_std(ret,20)) + 0.5)`
- **意**: 单位风险的信号强度
- **据**: [推测] | **向**: + | **数**: 现有

### NL25 元因子：本票最适合的因子类型
- **式**: `argmax_window_IC(ret 的各窗口自相关)`——定义为：若 ts_corr(ret,ts_delay(ret,1),60)>0 则取动量类信号，否则取反转类
- **意**: 元层选择（个股自适应因子路由；实现复杂度高，思想实验性质）
- **据**: [推测] | **向**: ? | **数**: 现有
