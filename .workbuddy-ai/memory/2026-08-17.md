# 2026-08-17 quantlab 定时预测任务搭建

## 后续重大事件（8/17 深夜 ~ 8/18 凌晨，zcode 重构 + 我的对抗性复检）
- **三阶段重构**（commits 1f146fb/86049a0/7e78494）+ 修复 commit 6b5aefe：
  - 根因一修复：`get_lookback_start` 改为 DISTINCT 日期窗口（实测 08-17 → 2025-07-23，恰 260 交易日）
  - 根因二发现：GZ2000 九列特征计算代码从未入 git（_load_index_data 构建后未 join）——按语义重建公式补回
  - 数据修复：`_recompute_factors_range.py` 重算 2026-07-13~08-17 全部 28814 行
  - 新增 `factors/integrity.py`（硬失败/软警告）+ `data/pull.py` 统一拉取入口 + `data/calendar.py` 交易日历
- **对抗性复检结论（全部实测）**：
  - lookback 修复 ✅ 污染区间 7 因子非空率 100%；值级独立复算偏差 0.00%；确定性 bit 级一致；空跑/integrity exit 0
  - 遗留无害差异：入模 alpha50_v0 ~1e-3 微差（浮点并列排名换位）、alpha60_v0 3 只换档——cs_rank 对浮点噪声敏感，可接受
  - 不入模的 alpha85 窗口敏感（1088 只差 1 个排名量子）——不影响预测；LOOKBACK_DAYS=260 对个别深嵌套因子理论边缘不足
- **定时任务已被同步更新**：每工作日 19:30（原 17:08），第一步改用新入口 `python -m data.pull`（pull_adj.py 已弃用）
- 修复后重生成 08-17 报告：Top5 完全洗牌（牧高笛/兰州黄河/江山欧派/上海三毛/赛福天，分数 0.036~0.037 vs 修复前 0.018~0.025）——**修复前 7/13~8/17 的报告作废**

## 完成事项
1. **Mac 运行环境搭建**：项目原在 Windows 跑（项目内 `.venv` 是 Windows 的，勿动）。
   - 新建隔离环境：`/Users/cui/.workbuddy-ai/quantlab-env/`（托管 Python 3.13.12）
   - 依赖：requirements.txt 全量 + 补装 `pyarrow`（pandas→polars 转换必需，requirements 里缺失）
   - 系统依赖：`brew install libomp`（LightGBM 的 dylib 需要）
2. **三步流水线验证跑通**（2026-08-17 21:13~21:16）：
   - `python data/pull_adj.py`：补齐 08-11~08-17 共 5 个交易日（每日约 5540 行行情+估值+筹码）
   - `python -m factors.update`：155 因子增量计算，5539 行入 factor_values
   - `python forecast_display/generate_lgb.py`：生成 2026-08-17 预测 HTML
3. **定时任务已建**：`quantlab 每日预测流水线`（id: automation-1786972402049）
   - 每个工作日 17:08，ACTIVE，cwd=/Users/cui/Projects/quantlab
   - 三步顺序执行：拉数据→增量因子→预测 HTML，失败即停，非交易日自动空转

## 关键结论
- Python 解释器统一用 `/Users/cui/.workbuddy-ai/quantlab-env/bin/python`
- 流水线全程约 40 秒（数据 30s + 因子 6s + 预测 4s）
- 最新报告：`forecast_display/html_lgb/mainboard_microcap/2026-08-17_forecast_lgb.html`
