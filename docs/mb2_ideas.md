# mb2 灵感记录（临时文件）

> **生命周期**：本文件是 mb2（全主板清单第二版）筹备期的灵感速记，**mb2 完成定稿后整文件删除**（用户裁定 2026-08-27）。
> 正式裁定史与实验全记录在 agent memory；spec/plan 见 `docs/superpowers/`。

## 1. gap1d 特征列换引擎：nn_gap1d → gb_gap1d（✅ 已执行 2026-08-28：mb1 方案级转向，本条不再是 mb2 待办）

- **内容**：把清单里的神经网络因子 **nn_gap1d（MLP）替换为 XGBoost 运算的 gb_gap1d**。
- 用户原话要点："把 gap1d 从神经网络运算的 Gap1D 改成 XGBoost 运算的 Gap1D。"（初述 LightGBM，同日更正为 XGBoost）
- **裁定史**：2026-08-27 口述灵感 → 2026-08-28 首次裁定"下次不要用 NN 版，改用 XGBoost 版" → 同日二次澄清升级：**"这不是一次性决定，是 mb1 方案整体转向 Boost 族模型因子"，全方案范围（主清单+折清单）替换**。
- **已执行（2026-08-28 当日）**：`build_gb_gap1d --pool mainboard_all` 全量构建 **3.5 分钟**完成（对照：nn 稀疏版 20.5 分钟；nn 致密版 60+ 分钟未完成、经用户授权杀掉）——**OOS rank IC +0.1813**（IR 2.08，t=90，1871 日，2018-12 起），写入 576.3 万行覆盖至当日前沿，2020+ 覆盖率 87%（XGBoost 原生容忍 NaN 输入，无 nn 的 feat_ok 杀行问题）。清单替换：主清单 6d/open2d + 折清单 14 份（F1-F7 × 6d/open2d），方案内零 nn_gap1d 残留，json note 均留裁定史。nn_gap1d 稀疏列（106 万行）留存表内，无消费方，未删。
- **性能教训（nn 路径，供未来参考）**：nn 脚本逐年 IC 统计用 `df_ok.xs(d)` 逐日全表切片，O(日数×总行数)，350 万行时被交换放大到 1 小时以上；如未来重建 nn，需先改为按日 groupby 预计算行数。MLP 峰值内存（面板 ~4GB + fit 内部复制 2~4GB）在 32GB 机器上引发系统交换、整机卡顿——换 Boost 族的工程依据。
